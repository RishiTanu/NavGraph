<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
   >

    <!-- Main content layout -->
    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">
        <!-- Main content goes here, like a button to open/close drawers -->
        <Button
            android:id="@+id/button_open_first_drawer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Open First Drawer"
            android:layout_gravity="center"/>
        <Button
            android:id="@+id/button_open_second_drawer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Open Second Drawer"
            android:layout_gravity="center|bottom"/>
    </FrameLayout>

    <!-- First right-side drawer layout -->
    <LinearLayout
        android:id="@+id/first_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="vertical"
        android:background="@android:color/white">
        <!-- Drawer content goes here -->
    </LinearLayout>

    <!-- Second right-side drawer layout -->
    <LinearLayout
        android:id="@+id/second_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="vertical"
        android:background="@android:color/holo_blue_light">
        <!-- Drawer content goes here -->
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
